{{each data}}
<div class="in-jc subject-single {{$value.active === 1 ? 'selected' : ''}}" data-subjectid="{{$value.subject_id}}">
  <span class="num">{{$value.subject_num}}</span>
  <span class="name">{{$value.subject_name}}</span>
</div>
{{/each}}